# Copy this file to `.env.production` (or set these variables in Railway).
# Retrieve your OpenAI API key from https://platform.openai.com/
OPENAI_API_KEY=

# Optional: pre-seed MCP tokens via env and surface them in the UI later.
# For now the MCP modal accepts headers interactively, so this is informational.
# MCP_ZAPIER_BEARER=
